import * as anchor from "@coral-xyz/anchor";

main();
async function main() {
  // Assume Bonkers program is deployed to local validator with ADMIN key
  // 1. Load Admin Key
  // 2. Load Bob Key
  // 3. Load Bonkers Program
  // Start a simulation for X duration with Y intervals. Initialize the Bonkers game, then randomly add in sleighs that are staking. Make rolls every interval, then claim for each sleigh that's staked.
}
